openapi: 3.0.2
info:
  title: MoviePumpkins
  description: MoviePumpkins
  version: 1.0.0
paths: { }
components:
  parameters:
    IdInPath:
      required: true
      in: path
      schema:
        type: string
        format: uuid
      name: id
  responses:
    400:
      description: bad request
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiServiceErrorDto"
    401:
      description: login required
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiServiceErrorDto"
    403:
      description: user not authorized
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiServiceErrorDto"
    404:
      description: resource not found
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiServiceErrorDto"
    409:
      description: resource already exists
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiServiceErrorDto"
  schemas:
    ApiServiceErrorDto:
      type: object
      required:
        - code
        - message
      properties:
        code:
          type: string
        message:
          type: string
        details:
          type: object
          additionalProperties: true
    IdDto:
      type: object
      required:
        - id
      properties:
        id:
          type: string
          format: uuid