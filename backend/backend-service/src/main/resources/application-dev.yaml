moviepumpkins:
  integration:
    keycloak:
      auth-server-url-base: http://localhost:9090
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pumpkins
    username: admin
    password: gYLWLbQUcY
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9090/realms/moviepumpkins
      client:
        provider:
          keycloak:
            token-uri: http://localhost:9090/realms/moviepumpkins/protocol/openid-connect/token
        registration:
          keycloak:
            client-id: moviepumpkins-backend-core
logging:
  level:
    net.moviepumpkins.core: debug